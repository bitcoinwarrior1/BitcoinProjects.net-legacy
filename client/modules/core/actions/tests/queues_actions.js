const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../queues_actions';

describe('core.actions.queues_actions', () => {
  it('should do something');
});
